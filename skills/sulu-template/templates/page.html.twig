{% extends 'base.html.twig' %}

{% block content %}
    <h1>{{ content.title }}</h1>

    {% for block in content.blocks %}
        {% if block.type == 'editor' %}
            {{ block.article|raw }}
        {% endif %}
    {% endfor %}
{% endblock %}
